{%- comment -%}
  Bloque: anchor-section.liquid
  Propósito: Agregar un punto de ancla invisible a cualquier sección del tema.
  Se puede agregar desde el editor de temas sin modificar el código original de la sección.
{%- endcomment -%}

{%- if block.settings.anchor_id != blank -%}
  <div 
    id="{{ block.settings.anchor_id | handle }}" 
    class="section-anchor"
    data-scroll-offset="{{ block.settings.scroll_offset }}"
    aria-hidden="true">
    {%- comment -%} Este div es un marcador invisible para el scroll. {%- endcomment -%}
  </div>
{%- endif -%}

{% schema %}
{
  "name": "Punto de Ancla",
  "target": "section",
  "settings": [
    {
      "type": "header",
      "content": "⚓ Configuración del Ancla"
    },
    {
      "type": "text",
      "id": "anchor_id",
      "label": "ID del Ancla",
      "info": "Identificador único para esta sección. Ejemplo: 'desarrollo'. Este ID se usará en el enlace del menú (ej: /#desarrollo)."
    },
    {
      "type": "number",
      "id": "scroll_offset",
      "label": "Ajuste de scroll (offset)",
      "info": "Ajuste fino para esta ancla específica. Anula el offset global. Usa un valor negativo para subir o positivo para bajar.",
      "default": 0
    }
  ]
}
{% endschema %}